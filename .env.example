# Members Enquiries System - Environment Configuration Example
# Copy this file to .env and fill in your actual values
# NOTE: .env is in .gitignore and should NOT be committed to version control

# Django Settings
# Options: development | test | production
DJANGO_SETTINGS_MODULE=project.settings.development

# Environment indicator (used in templates and logging)
# Options: DEVELOPMENT | TEST | PRODUCTION
ENVIRONMENT=DEVELOPMENT

# Core Configuration
# ====================

# Council name - displayed in footer and branding
# This allows the same codebase to be used by different councils
COUNCIL_NAME=Redcar & Cleveland Borough Council

# Domain - used for ALLOWED_HOSTS, CSRF, and CORS settings
# For local development: localhost
# For test environment: membersenquiries-test.redclev.net
# For production: members2.redclev.net
DOMAIN=localhost

# Django Secret Key (production only - required for test and production settings)
# Generate a new key: python manage.py shell -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# DJANGO_SECRET_KEY=your-secret-key-here

# Site Domain (optional - defaults to localhost:8000)
# SITE_DOMAIN=localhost:8000

# Database Configuration
# ====================
# For development: SQLite (no configuration needed, uses db.sqlite3)
# For test/production: Microsoft SQL Server

# DATABASE_ENGINE=mssql
# DATABASE_NAME=your_database_name
# DATABASE_USER=your_database_user
# DATABASE_PASSWORD=your_database_password
# DATABASE_HOST=your.sql.server.address
# DATABASE_PORT=1433

# Azure Entra ID / Microsoft Authentication
# ====================
# Get these values from your Azure AD / Microsoft Entra ID application registration
# Azure portal: https://portal.azure.com > Azure Entra ID > App registrations

# AZURE_CLIENT_ID=your-azure-client-id
# AZURE_CLIENT_SECRET=your-azure-client-secret
# AZURE_TENANT_ID=your-azure-tenant-id

# Email Configuration (Optional)
# ====================
# For development: typically not needed (console backend)
# For production: configure email service

# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=your-smtp-server
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@example.com
# EMAIL_HOST_PASSWORD=your-email-password
